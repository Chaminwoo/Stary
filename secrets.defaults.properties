# 기본 플레이스홀더 — secrets-gradle-plugin 이 secrets.properties 부재 시 사용.
# 실제 값은 secrets.properties(=git 추적 제외)에 넣는다. 이 파일에는 절대 실제 키를 넣지 말 것.

# Google Maps SDK API Key (AndroidManifest 의 com.google.android.geo.API_KEY 로 주입)
# TODO: 실제 키로 교체하지 말고, secrets.properties 에 별도로 설정할 것.
MAPS_API_KEY=TODO_ADD_YOUR_GOOGLE_MAPS_API_KEY
