# XcodeGen 이 생성(커밋 금지)
*.xcodeproj
*.xcworkspace

# 빌드 산출물
build/
DerivedData/
*.ipa
*.xcarchive

# CocoaPods / SPM
Pods/
.build/

# 비밀 — 절대 커밋 금지
GoogleService-Info.plist
# 예외: 유일한 정본(2026-07-15 사용자 지시 — Firebase iOS 클라이언트 설정은 앱 바이너리에 항상 포함되는
# 값이라 실질적 비밀이 아님). 다른 위치(레포 루트, iosApp/ 바로 밑)에는 절대 두지 말 것.
!Sources/GoogleService-Info.plist
*.p8
*.p12
*.mobileprovision

# fastlane 산출
iosApp/fastlane/report.xml
fastlane/README.md
