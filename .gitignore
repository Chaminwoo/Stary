*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
# 루트 및 모든 모듈(:shared, :androidApp 등)의 빌드 산출물
/build
build/
/captures
.externalNativeBuild
.cxx
local.properties

# --- 민감값 (절대 커밋 금지) ---
secrets.properties
# 템플릿/기본값 파일도 추적 제외 (과거 실수로 실제 키가 들어가 히스토리에서 제거함)
secrets.properties.example
secrets.defaults.properties
# 실제 Firebase 설정 (템플릿 google-services.json.example 만 커밋)
/androidApp/google-services.json
# 릴리즈 서명 (절대 커밋 금지 — 분실 시 앱 업데이트 영구 불가)
keystore.properties
*.jks
*.keystore
# 디버그용 임시 스크린샷
.tmp_screen.png

# Kotlin/Native, KMP
.kotlin

# iOS (추후 Xcode 프로젝트 추가 시)
xcuserdata/
*.xcworkspace/xcuserdata/
Pods/

# Firebase CLI 배포 캐시(호스팅 업로드 해시) — 로컬 산출물이라 커밋 불필요
.firebase/
